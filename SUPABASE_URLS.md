# Настройка Redirect URLs в Supabase

## 🔧 Обязательные настройки для локальной разработки:

Добавьте в **Supabase Dashboard** → **Authentication** → **URL Configuration** → **Redirect URLs**:

```
http://localhost:5174/auth/callback
http://localhost:5174/*
http://127.0.0.1:5174/auth/callback
http://127.0.0.1:5174/*
```

**Важно:** Если вы используете другой порт (не 5174), замените его на ваш порт.

## 🌐 Production настройки:

### Redirect URLs:
```
https://toqibox.win/auth/callback
https://toqibox.win/*
```

### Site URL:
```
https://toqibox.win
```

## 📦 Cloudflare Pages (опционально):

Если используете Cloudflare Pages, добавьте также:
```
https://811182a5.toqibox.pages.dev/auth/callback
```

Или можно использовать wildcard:
```
https://*.toqibox.pages.dev/*
```

## ✅ Проверка локальной разработки:

1. Запустите `npm run dev`
2. Откройте `http://localhost:5174`
3. Войдите через Google или почту
4. После входа вы должны остаться на `http://localhost:5174/...`, а не перенаправляться на production

## 📝 Подробная инструкция:

См. файл `LOCAL_DEVELOPMENT.md` для детальной инструкции по локальной разработке.

