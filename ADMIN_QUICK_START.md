# Быстрый старт админ-панели

## ✅ Что уже сделано:

1. ✅ **Проверка авторизации и прав доступа** - админка защищена
2. ✅ **Поиск и фильтрация** - можно искать по всем разделам
3. ✅ **Улучшенное отображение пользователей** - показывается email и статистика
4. ✅ **Выбор количества дней** - при подтверждении платежа можно выбрать дни
5. ✅ **Все платежи** - показываются все статусы (pending, approved, rejected)
6. ✅ **Редактирование артистов** - можно редактировать имя и slug
7. ✅ **Статистика по трекам** - добавлена в общую статистику

## 🚀 Что нужно сделать для запуска:

### 1. Создать таблицу админов

Выполните в Supabase SQL Editor:

```sql
-- Скопируйте содержимое файла sql/create_admins_table.sql
```

### 2. Добавить первого администратора

```sql
-- Замените YOUR_USER_ID на ваш ID из auth.users
-- Замените your-email@example.com на ваш email
INSERT INTO public.admins (user_id, email, is_active)
VALUES (
  'YOUR_USER_ID',
  'your-email@example.com',
  true
);
```

**Как найти YOUR_USER_ID:**
1. Войдите в приложение
2. Откройте консоль браузера (F12)
3. Выполните: `(await supabase.auth.getSession()).data.session.user.id`
4. Скопируйте UUID

### 3. Для локальной разработки

В локальной разработке админка доступна без проверки (для удобства). В production обязательно нужна таблица `admins` и добавление администраторов.

## 📋 Новые возможности:

- **Поиск** - работает во всех разделах
- **Фильтры** - по статусу платежей и типу премиума
- **Модальные окна** - для подтверждения платежей и редактирования
- **Статистика** - добавлено количество треков
- **Ссылки** - можно перейти на страницу артиста
- **Расширенное продление** - +7, +14, +21, +30 дней
- **Все платежи** - видно все статусы, не только pending

## 🔒 Безопасность:

- Проверка авторизации обязательна
- Проверка прав доступа через таблицу `admins`
- В локальной разработке доступ разрешен для удобства
- В production только админы могут зайти

