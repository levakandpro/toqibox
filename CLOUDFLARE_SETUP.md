# Настройка Cloudflare Pages для TOQIBOX

## ✅ Что уже настроено:

1. **Домен:** toqibox.win
2. **Переменные окружения:**
   - `VITE_SUPABASE_URL` ✅
   - `VITE_SUPABASE_ANON_KEY` ✅
3. **Деплой:** Успешный ✅

## 🔧 Что нужно проверить/настроить:

### 1. Файл `_redirects` для SPA роутинга

Файл `_redirects` уже создан в корне проекта. Он нужен для правильной работы React Router на Cloudflare Pages.

**Важно:** Убедитесь, что файл `_redirects` находится в папке `public/` или будет скопирован в `dist/` при сборке.

### 2. Настройки Supabase (ОБЯЗАТЕЛЬНО!)

Зайдите в Supabase Dashboard → Authentication → URL Configuration:

**Site URL:**
```
https://toqibox.win
```

**Redirect URLs (добавьте все):**
```
https://toqibox.win/auth/callback
https://toqibox.win/*
https://811182a5.toqibox.pages.dev/auth/callback
```

### 3. Настройки сборки в Cloudflare Pages

В настройках проекта Cloudflare Pages проверьте:

**Build settings:**
- **Build command:** `npm ci && npm run build`
- **Build output directory:** `dist`
- **Root directory:** `/` (корень проекта)
- **Node version:** `20` (указано в `.nvmrc`)

**Важно:** Если деплой не запускается ("No deployment available" или "unable to submit build job"):

1. **Проверьте подключение к репозиторию:**
   - Settings → Source → убедитесь, что репозиторий подключен
   - Проверьте, что ветка `main` отслеживается
   - Попробуйте переподключить репозиторий

2. **Упростите build command:**
   - Попробуйте сначала: `npm run build` (без `npm ci`)
   - Если не работает, попробуйте: `npm install && npm run build`

3. **Проверьте переменные окружения:**
   - Settings → Environment variables
   - Убедитесь, что `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` установлены
   - Проверьте, что они применяются к Production

4. **Проверьте Node version:**
   - В настройках сборки выберите Node.js версию 20 или выше
   - Или оставьте "Auto" для автоматического определения

5. **Попробуйте Retry deployment:**
   - В разделе Deployments нажмите "Retry deployment"
   - Иногда помогает при временных проблемах с сетью

6. **Проверьте логи:**
   - Откройте последний deployment
   - Посмотрите полные логи сборки
   - Ошибка может быть в другом месте, не только в "Initializing"

### 4. Проверка после деплоя

После следующего деплоя проверьте:

1. ✅ Главная страница открывается: `https://toqibox.win`
2. ✅ Авторизация через Google работает
3. ✅ Редирект после логина работает
4. ✅ Создание страницы артиста работает
5. ✅ Редактирование на странице артиста работает

### 5. Если что-то не работает:

**Проблема: 404 на всех страницах кроме `/`**
- Проверьте, что файл `_redirects` в папке `public/`
- Или добавьте в настройках Cloudflare Pages: "Single Page Application" = ON

**Проблема: Ошибка авторизации**
- Проверьте Redirect URLs в Supabase
- Убедитесь, что домен `toqibox.win` добавлен в Supabase

**Проблема: Переменные окружения не работают**
- В Cloudflare Pages переменные должны начинаться с `VITE_`
- После изменения переменных нужен новый деплой

## 📝 Следующие шаги:

1. Добавьте Redirect URLs в Supabase (см. выше)
2. Сделайте новый коммит и пуш в репозиторий
3. Cloudflare автоматически задеплоит новую версию
4. Проверьте работу на `https://toqibox.win`

