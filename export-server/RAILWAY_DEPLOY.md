# 🚂 Деплой export-server на Railway

## Быстрый старт

1. **Создайте проект на Railway:**
   - Зайдите на https://railway.app
   - New Project → Deploy from GitHub repo
   - Выберите репозиторий `levakandpro/toqibox`
   - В настройках проекта укажите:
     - **Root Directory:** `export-server`
     - **Start Command:** `npm start`

2. **Railway автоматически:**
   - Установит зависимости (включая `ffmpeg-static`)
   - Запустит сервер на порту из переменной `PORT`

3. **Получите URL:**
   - Railway даст домен вида: `https://toqibox-export-production.up.railway.app`
   - Или создайте кастомный домен в настройках

4. **Настройте переменные окружения (если нужно):**
   - `PORT` - порт сервера (Railway установит автоматически)

5. **Добавьте URL в Cloudflare Pages:**
   - В настройках проекта Cloudflare Pages
   - Environment Variables → Production
   - Добавьте: `VITE_EXPORT_API_URL = https://your-railway-domain.railway.app`

## Проверка работы

После деплоя проверьте endpoints:

```bash
# Проверка здоровья (должен вернуть 404 или ошибку - это нормально, главное не 500)
curl https://your-railway-domain.railway.app/export

# Должен вернуть 404 или ошибку валидации (не 500)
```

## Структура проекта

```
export-server/
├── server.js          # Основной сервер
├── package.json       # Зависимости (включая ffmpeg-static)
├── nixpacks.toml      # Конфигурация Railway (опционально)
└── README.md          # Документация
```

## Endpoints

- `POST /export` - Создать задачу экспорта (FormData: audio, photo, duration, plan)
- `GET /export/:jobId` - Проверить статус (queued | processing | completed | failed)
- `GET /download/:jobId` - Скачать готовый MP4

## Технические детали

- **FFmpeg:** Используется `ffmpeg-static` (гарантированно работает на Railway)
- **Порт:** Автоматически определяется Railway через переменную `PORT`
- **Временные файлы:** Сохраняются в `/tmp/toqibox-exports/` и автоматически удаляются после скачивания
