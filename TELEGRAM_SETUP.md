# Настройка Telegram-уведомлений для TOQIBOX/TQ STUDIO

## Обзор

Реализована система Telegram-уведомлений для заявок на оплату с возможностью одобрения/отклонения прямо из Telegram.

## Что реализовано

1. **Уведомления о новых заявках** (`/api/tg/notify-payment-request`)
   - Автоматическая отправка уведомления в Telegram при создании новой заявки
   - Отправка чека (photo/document) вместе с уведомлением
   - Inline кнопки для быстрого одобрения/отклонения

2. **Webhook для обработки действий из Telegram** (`/api/tg/webhook`)
   - Обработка нажатий кнопок "Одобрить"/"Отклонить"
   - Автоматическое обновление статуса заявки и активация подписки
   - Идемпотентность: повторное нажатие не выполняет действие второй раз

3. **Уведомления о регистрации** (`/api/tg/notify-new-user`) - опционально
   - Уведомления о новых пользователях (можно включить/выключить через env)

## Настройка

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **BOT_TOKEN** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID админа

1. Начните диалог с вашим ботом в Telegram
2. Отправьте боту любое сообщение (например: `/start`)
3. Откройте в браузере: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
   Замените `<BOT_TOKEN>` на ваш токен
4. Найдите в ответе `"chat":{"id":123456789}` - это ваш **CHAT_ID**
5. Сохраните этот ID

### 3. Настройка переменных окружения

Добавьте следующие переменные окружения в Cloudflare Pages (Settings → Environment Variables):

```
TELEGRAM_BOT_TOKEN=ваш_bot_token
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
SUPABASE_URL=https://ваш_проект.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
TELEGRAM_NOTIFY_NEW_USERS=true  # опционально: включить уведомления о регистрации (по умолчанию true)
```

**ВАЖНО:**
- `SUPABASE_SERVICE_ROLE_KEY` - это ключ с полными правами доступа, НЕ публичный ключ!
- Получить service role key можно в Supabase Dashboard → Settings → API → Service Role Key
- **НЕ** публикуйте этот ключ в клиентском коде!

### 4. Настройка Webhook для Telegram

Telegram webhook нужно настроить через Bot API. Выполните следующую команду:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://ваш_домен.com/api/tg/webhook"
  }'
```

Замените:
- `<BOT_TOKEN>` на ваш токен бота
- `https://ваш_домен.com` на ваш домен (например: `https://toqibox.pages.dev`)

**Проверка webhook:**
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

Ответ должен содержать ваш URL в поле `url`.

**Удаление webhook (если нужно):**
```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/deleteWebhook"
```

### 5. Проверка работы

1. Создайте тестовую заявку на оплату через форму (TOQIBOX или Studio)
2. После успешного сохранения должно прийти уведомление в Telegram:
   - Текст с деталями заявки
   - Чек (если загружен)
   - Кнопки "✅ Одобрить" и "❌ Отклонить"
3. Нажмите кнопку "Одобрить" - должно:
   - Обновиться сообщение с подтверждением
   - Активироваться подписка в БД (30 дней для PREMIUM, 1 год для PREMIUM+)
   - Заявка должна исчезнуть из вкладки "Заявки на оплату" и появиться в "Одобренные"
4. Попробуйте нажать кнопку повторно - должно показать "⚠️ Заявка уже одобрена"

## Структура endpoints

### POST /api/tg/notify-payment-request
**Вызывается клиентом** после успешного INSERT payment_request.

**Request body:**
```json
{
  "payment_request_id": "uuid"
}
```

**Response:**
```json
{
  "success": true,
  "message_id": 123,
  "sent": true
}
```

### POST /api/tg/webhook
**Вызывается Telegram Bot API** при нажатии кнопок в сообщении.

**Request body:** (Telegram Update object)
```json
{
  "callback_query": {
    "id": "...",
    "data": "{\"action\":\"approve\",\"request_id\":\"uuid\",\"product\":\"studio\"}",
    "message": { ... }
  }
}
```

**Response:** `200 OK` или ошибка

### POST /api/tg/notify-new-user (опционально)
**Вызывается клиентом** после успешной регистрации/логина.

**Request body:**
```json
{
  "user_id": "uuid",
  "email": "user@example.com"
}
```

## Безопасность

1. **Защита webhook по chat_id**: webhook принимает только апдейты из админского чата (`TELEGRAM_ADMIN_CHAT_ID`)
2. **Service role key только на сервере**: ключ используется только в Cloudflare Functions, никогда не передается клиенту
3. **Идемпотентность**: все операции approve/reject проверяют статус перед выполнением, повторное нажатие не выполняет действие
4. **Fail closed**: при любой ошибке операция не выполняется, отправляется уведомление об ошибке

## Формат сообщений в Telegram

### Новое уведомление о заявке:
```
💰 Новая заявка на оплату
📦 Продукт: TOQIBOX (или TQ STUDIO)
💎 Тариф: PREMIUM (или PREMIUM_PLUS)
💵 Сумма: 160 TJS
👤 user@example.com
🆔 ID: abc12345...

[Inline кнопки: ✅ Одобрить | ❌ Отклонить]
[Чек (photo или document)]
```

### После одобрения:
```
✅ Подтверждено
📦 TQ STUDIO
💎 PREMIUM
👤 user@example.com
✅ Активен до 10.02.2026 (30 дней)
```

### После отклонения:
```
❌ Отклонено
📦 TOQIBOX
💎 PREMIUM_PLUS
👤 user@example.com
```

## Устранение неполадок

### Уведомления не приходят
1. Проверьте переменные окружения в Cloudflare Pages
2. Проверьте, что `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ADMIN_CHAT_ID` установлены правильно
3. Проверьте логи в Cloudflare Pages (Functions → Logs)

### Webhook не работает (кнопки не реагируют)
1. Проверьте, что webhook настроен: `curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"`
2. Проверьте, что URL webhook указывает на правильный домен
3. Проверьте, что `TELEGRAM_ADMIN_CHAT_ID` совпадает с вашим chat_id
4. Проверьте логи Cloudflare Functions

### Ошибки при одобрении/отклонении
1. Проверьте, что `SUPABASE_SERVICE_ROLE_KEY` правильный (не публичный ключ!)
2. Проверьте, что RLS политики разрешают обновление `payment_requests` с service role
3. Проверьте логи Cloudflare Functions для деталей ошибки

### Чек не отправляется
1. Проверьте, что `receipt_url` доступен и файл можно скачать
2. Проверьте формат файла (png, jpg, jpeg, webp, pdf)
3. Если файл недоступен, отправляется только ссылка в тексте

## Дополнительные настройки

### Отключить уведомления о регистрации
Установите в env:
```
TELEGRAM_NOTIFY_NEW_USERS=false
```

### Изменить текст сообщений
Отредактируйте файлы:
- `functions/api/tg/notify-payment-request.js` - текст уведомления о заявке
- `functions/api/tg/webhook.js` - текст подтверждений
- `functions/api/tg/notify-new-user.js` - текст уведомления о регистрации
