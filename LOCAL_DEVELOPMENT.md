# Локальная разработка TOQIBOX

## Настройка Supabase для локальной разработки

### 1. Добавьте Redirect URLs в Supabase

Зайдите в **Supabase Dashboard** → **Authentication** → **URL Configuration** → **Redirect URLs** и добавьте:

```
http://localhost:5174/auth/callback
http://localhost:5174/*
http://127.0.0.1:5174/auth/callback
http://127.0.0.1:5174/*
```

**Важно:** Если вы используете другой порт (не 5174), замените его на ваш порт.

### 2. Site URL в Supabase

В том же разделе **Site URL** можно оставить:
```
https://toqibox.win
```

Или для локальной разработки можно временно установить:
```
http://localhost:5174
```

### 3. Как проверить, что настройки работают

1. Запустите локальный сервер:
   ```bash
   npm run dev
   ```

2. Откройте в браузере:
   ```
   http://localhost:5174
   ```

3. Попробуйте войти через Google или почту

4. После входа вы должны остаться на локальном адресе (`http://localhost:5174/...`), а не перенаправляться на `https://toqibox.win`

### 4. Как открыть страницы локально

После входа вы можете открыть:

- **Страницу автора:** `http://localhost:5174/a/your-slug`
- **Страницу трека:** `http://localhost:5174/t/your-slug`
- **Кабинет автора:** `http://localhost:5174/author`
- **Админ-панель:** `http://localhost:5174/admin`

**Важно для админки:** Админ-панель не требует входа, но для работы с данными нужно, чтобы RLS политики в Supabase разрешали чтение таблиц `artists` и `payments`. В локальной разработке это может не работать из-за RLS. Для тестирования можно временно отключить RLS или настроить политики для анонимного доступа.

### 5. Если всё равно редиректит на production

Если после входа вас всё равно перенаправляет на `https://toqibox.win`:

1. **Проверьте консоль браузера** - там должны быть логи о редиректе
2. **Очистите localStorage:**
   ```javascript
   localStorage.clear();
   ```
3. **Проверьте, что в Supabase добавлены правильные Redirect URLs**
4. **Проверьте, что вы используете правильный порт** (обычно 5174 для Vite)

### 6. Отладка

В консоли браузера вы увидите логи:
```
🔀 Redirecting after login: { raw: "...", path: "...", next: "...", isLocal: true, ... }
```

Если `isLocal: false`, значит код не распознаёт локальную среду. Проверьте `window.location.hostname`.

### 7. Альтернативный способ (если ничего не помогает)

Если Supabase всё равно редиректит на production, можно временно изменить Site URL в Supabase на:
```
http://localhost:5174
```

**Важно:** Не забудьте вернуть обратно на `https://toqibox.win` перед деплоем!

